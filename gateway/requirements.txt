# Façade runtime deps. Pin tighter at deploy after validating the versions you
# build with (mirror the pinning discipline in docs/litellm-supply-chain.md).
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
