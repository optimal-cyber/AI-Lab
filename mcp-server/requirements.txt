# Runtime deps — pin tighter at deploy after validating against the MCP SDK
# version you build with (FastMCP streamable-http API).
mcp>=1.8
httpx>=0.27
tenacity>=8.2
structlog>=24.1
pydantic>=2.6
uvicorn[standard]>=0.29
starlette>=0.37
